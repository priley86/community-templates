# Frontend Production Environment Variables
# Copy this to .env.production and update with your values
# Note: With nginx proxy, API calls go to same origin (frontend URL)
# Nginx forwards /api/** to the backend Cloud Run service

# Backend URL for nginx proxy configuration
BACKEND_URL=
