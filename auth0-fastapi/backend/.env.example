# REQUIRED: Copy this file to .env and fill in your values
# In Firebase Studio: Create backend/.env after workspace creation

# NOTE: APP_BASE_URL, LANGGRAPH_API_URL, and CORS origins are set in dev.nix
# You only need to set the secret values below

# Auth0 Configuration (Get from Auth0 Dashboard)
# Generate AUTH0_SECRET with: openssl rand -hex 32
AUTH0_SECRET="use [openssl rand -hex 32] to generate a 32 bytes value"
AUTH0_DOMAIN="{yourDomain}"
AUTH0_CLIENT_ID="{yourClientId}"
AUTH0_CLIENT_SECRET="{yourClientSecret}"

# LLM API Key (required for the AI assistant to work)
# For Google Gemini (default): Get your key at https://aistudio.google.com/app/apikey
# For OpenAI: Get your key at https://platform.openai.com/api-keys
# This template supports both - see GEMINI.md for switching between providers
GOOGLE_API_KEY=""

# LangGraph Configuration (Optional - for production deployment)
# When deploying to Cloud Run, set LANGGRAPH_EXTERNAL_URL to your deployed LangGraph service
# Leave blank for local development (will use localhost:54367)
# LANGGRAPH_EXTERNAL_URL=https://langgraph-server-xxx-uc.a.run.app
# LANGGRAPH_API_KEY=  # Optional: API key if using authenticated LangGraph
